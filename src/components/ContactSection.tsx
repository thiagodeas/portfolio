
import { ContactForm } from "./ContactForm"

export const ContactSection = () => {
  return (
    <section id="contato" className="custom-container">
      <div>
        <h2 className="text-title font-bold">Contato</h2>
        <ContactForm />
      </div>

    </section>
  )
}